import { StarsRatingPipe } from './stars-rating.pipe';

describe('StarsRatingPipe', () => {
  it('create an instance', () => {
    const pipe = new StarsRatingPipe();
    expect(pipe).toBeTruthy();
  });
});
